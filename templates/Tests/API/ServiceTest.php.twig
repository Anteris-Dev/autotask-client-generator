<?php

use Anteris\Autotask\API\{{ endpoint.plural }}\{{ endpoint.singular }}QueryBuilder;
use Tests\AbstractTest;

/**
 * @covers {{ endpoint.singular }}Service
 */
class {{ endpoint.singular }}ServiceTest extends AbstractTest
{
{% if resource.canBeQueried %}
    /**
     * @covers {{ endpoint.singular }}Service::query
     */
    public function testQuery()
    {
        $this->assertInstanceOf(
            {{ endpoint.singular }}QueryBuilder::class,
            $this->client->{{ endpoint.plural|first|lower }}{{ endpoint.plural|slice(1) }}()->query()
        );
    }
{% endif %}
}
